{
  "name": "lizzi.lib-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "clean": "npx lerna run clean",
    "build": "npx lerna run build",
    "pub": "npm run build && npx lerna publish && npx run clean"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
